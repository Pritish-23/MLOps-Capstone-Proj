apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone-proj
  labels:
    app: capstone-proj
spec:
  replicas: 2
  selector:
    matchLabels:
      app: capstone-proj
  template:
    metadata:
      labels:
        app: capstone-proj
    spec:
      containers:
      - name: capstone-proj
        image: 875285643722.dkr.ecr.us-east-1.amazonaws.com/capstone-proj:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "1"
        env:
        - name: CAPSTONE_TEST
          valueFrom:
            secretKeyRef:
              name: capstone-secret
              key: CAPSTONE_TEST

---
apiVersion: v1
kind: Service
metadata:
  name: capstone-proj-service
spec:
  type: LoadBalancer
  selector:
    app: capstone-proj
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000  